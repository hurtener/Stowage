# Binaries
/bin/
/dist/
/stowage
!cmd/stowage/
*.exe

# Test & coverage artifacts
*.test
*.out
coverage.html
coverage.json.tmp

# Local data
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Node (docs site / future UI)
node_modules/

# Eval runtime artifacts (fetched datasets, run results)
/eval/data/
/eval/results/*.json
/eval/results/*.jsonl

# Go workspace (local dev only — never committed; see go.work for rationale)
go.work
go.work.sum
