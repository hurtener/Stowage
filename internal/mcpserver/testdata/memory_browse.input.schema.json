{
  "additionalProperties": false,
  "properties": {
    "cursor": {
      "description": "Opaque continuation token from the preceding page. Omit for the first page.",
      "type": "string"
    },
    "limit": {
      "description": "Maximum results to return. Omit for the server default; request only the amount of context needed.",
      "type": "integer"
    },
    "mode": {
      "description": "Recent lists newest-created memories first; superseded lists replaced values oldest first.",
      "enum": [
        "",
        "recent",
        "superseded"
      ],
      "type": "string"
    }
  },
  "type": "object"
}
