{
  "additionalProperties": false,
  "properties": {
    "action": {
      "description": "Select one of this operation's supported actions. Mutating actions change durable state.",
      "enum": [
        "confirm",
        "reject"
      ],
      "type": "string"
    },
    "memory_id": {
      "description": "A memory ID returned by Stowage. Do not invent one.",
      "type": "string"
    }
  },
  "required": [
    "memory_id",
    "action"
  ],
  "type": "object"
}
