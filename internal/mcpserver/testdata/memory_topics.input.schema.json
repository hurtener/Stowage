{
  "additionalProperties": false,
  "properties": {
    "action": {
      "description": "Select one of this operation's supported actions. Mutating actions change durable state.",
      "enum": [
        "",
        "list",
        "upsert",
        "delete"
      ],
      "type": "string"
    },
    "key": {
      "description": "The existing topic or buffer key appropriate to this operation.",
      "type": "string"
    },
    "topics": {
      "description": "Topic definitions for explicit extraction-policy administration.",
      "items": {
        "additionalProperties": false,
        "properties": {
          "description": {
            "type": "string"
          },
          "key": {
            "description": "The existing topic or buffer key appropriate to this operation.",
            "type": "string"
          },
          "status": {
            "description": "The supported lifecycle state for this item; not a measure of real-world truth.",
            "type": "string"
          }
        },
        "required": [
          "key"
        ],
        "type": "object"
      },
      "type": [
        "null",
        "array"
      ]
    }
  },
  "required": [
    "action"
  ],
  "type": "object"
}
