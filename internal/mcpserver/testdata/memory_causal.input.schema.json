{
  "additionalProperties": false,
  "properties": {
    "depth": {
      "description": "Maximum causal traversal depth. Omit for the bounded server default.",
      "type": "integer"
    },
    "direction": {
      "description": "Traverse backward to causes, forward to effects, or both.",
      "enum": [
        "",
        "backward",
        "forward",
        "both"
      ],
      "type": "string"
    },
    "memory_id": {
      "description": "A memory ID returned by Stowage. Do not invent one.",
      "type": "string"
    }
  },
  "required": [
    "memory_id"
  ],
  "type": "object"
}
