{
  "description": "Correct an inspected memory using a newer exact user quotation. Supply its current revision from memory_inspect. The old value remains historical and rollback is possible; conversation history is not erased. A stale revision requires re-inspection, not a blind overwrite. Do not infer a correction the user did not establish.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {
      "expected_revision": {
        "description": "The revision returned by memory_inspect for the target's current state. Re-inspect after a conflict.",
        "pattern": "^[0-9a-f]{64}$",
        "type": "string"
      },
      "memory_id": {
        "description": "A memory ID returned by Stowage. Do not invent one.",
        "minLength": 1,
        "type": "string"
      },
      "quote": {
        "description": "Exact text from the user source, preserving qualifications and negation. Maximum 8192 UTF-8 bytes; generated summaries are not accepted.",
        "maxLength": 8192,
        "minLength": 1,
        "type": "string"
      },
      "source_record_id": {
        "description": "An existing user-source record ID returned by inspection or supplied by the host. It is validated within the caller's scope.",
        "type": "string"
      }
    },
    "required": [
      "memory_id",
      "expected_revision",
      "quote"
    ],
    "type": "object"
  },
  "name": "memory_correct",
  "outputSchema": {
    "additionalProperties": false,
    "properties": {
      "committed_at": {
        "type": "integer"
      },
      "current_status": {
        "type": "string"
      },
      "memory_id": {
        "type": "string"
      },
      "notice": {
        "type": "string"
      },
      "outcome": {
        "type": "string"
      },
      "receipt_id": {
        "type": "string"
      },
      "replaces_memory_id": {
        "type": "string"
      },
      "replayed": {
        "type": "boolean"
      },
      "retrieval_eligible": {
        "type": "boolean"
      },
      "revision": {
        "type": "string"
      },
      "source_record_id": {
        "type": "string"
      },
      "span_end": {
        "type": "integer"
      },
      "span_start": {
        "type": "integer"
      },
      "status_degraded": {
        "type": "boolean"
      }
    },
    "required": [
      "receipt_id",
      "memory_id",
      "outcome",
      "committed_at",
      "replayed",
      "current_status",
      "retrieval_eligible",
      "source_record_id",
      "span_start",
      "span_end",
      "notice"
    ],
    "type": "object"
  }
}
