# Keep the Docker build context lean — everything the CGo-free `go build` of
# ./cmd/stowage does NOT need. Go pulls modules from the proxy in the build stage.
.git
.github
*.md
docs/
eval/data/
data/
*.db
*.db-*
bin/
dist/
scripts/smoke/
test/
**/testdata/
.golangci.yml
.editorconfig
